<UserControl x:Class="ChartListModule.Views.ConfirmView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignWidth="300" Height="128">
    <Grid Height="128" VerticalAlignment="Top">

        <TextBox x:Name="textBox" HorizontalAlignment="Left" Height="78" Margin="10,10,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="280" Text="{Binding Notification}"/>
        <Button Content="OK" HorizontalAlignment="Left" Margin="10,93,0,0" VerticalAlignment="Top" Width="75" Command="{Binding addTextCommand}"/>
        <Button Content="Cancel" HorizontalAlignment="Left" Margin="90,93,0,0" VerticalAlignment="Top" Width="75" Command="{Binding CancelCommand}"/>

    </Grid>
</UserControl>